---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-service
  namespace: fintech
  labels:
    app: fraud-service
    app.kubernetes.io/name: fraud-service
    app.kubernetes.io/part-of: fintech
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fraud-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: fraud-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
      containers:
      - name: fraud-service
        image: ghcr.io/company/go-fintech/fraud-service:latest
        ports:
        - name: http
          containerPort: 8081
        env:
        - name: APP_NAME
          value: "fraud-service"
        - name: APP_PORT
          value: "8081"
        - name: KAFKA_BROKERS
          valueFrom:
            configMapKeyRef:
              name: kafka-config
              key: KAFKA_BROKERS
        - name: KAFKA_CONSUMER_GROUP
          value: "fraud-service"
        - name: KAFKA_TOPIC_TRANSACTIONS_PENDING
          value: "transactions.pending"
        - name: KAFKA_TOPIC_TRANSACTIONS_AUTHORIZED
          value: "transactions.authorized"
        - name: KAFKA_TOPIC_TRANSACTIONS_FAILED
          value: "transactions.failed"
        - name: KAFKA_TOPIC_FRAUD_EVENTS
          value: "fraud.events"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /readyz
            port: http
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /livez
            port: http
          initialDelaySeconds: 10
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: fraud-service
  namespace: fintech
  labels:
    app: fraud-service
spec:
  type: ClusterIP
  selector:
    app: fraud-service
  ports:
  - name: http
    port: 8081
    targetPort: http
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: fraud-service
  namespace: fintech
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: fraud-service
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: fraud-service
  namespace: fintech
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: fraud-service
  minReplicas: 2
  maxReplicas: 10
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
